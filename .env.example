# Backend environment variables
APP_ENV=development
ALLOWED_ORIGINS=http://localhost:3000,https://instatoolkit.vercel.app

# Redis (optional, for caching & rate limiting)
REDIS_URL=redis://localhost:6379

# Rate limiting
RATE_LIMIT_PER_MINUTE=20
